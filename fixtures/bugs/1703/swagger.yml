swagger: '2.0'
info:
  title: x-go-type bug demo
  version: 1.0.0
paths:
  '/users':
    get:
      operationId: getUsers
      summary: List all users
      responses:
        '200':
          description: Users list
          schema:
            $ref: '#/definitions/Users'
definitions:
  Users:
    type: array
    items:
      type: string
    x-go-type:
      import:
        package: "github.com/user/external_repo/my_pkg"
        alias: "my_pkg"
      type: "MyUsers"
