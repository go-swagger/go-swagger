swagger: "2.0"
info:
  title: foo api
  version: "1.0.0"
paths:
  /version:
    get:
      operationId: getVersion
      responses:
        "200":
          description: Success
          schema:
            type: string
