swagger: '2.0'
info:
  title: Some API
  description: Some API
  version: "1.0.0"

host: someapi.com

schemes:
  - https

basePath: /v1/

produces:
  - application/json
paths:
  /health_check:
    $ref: ./path/health_check.yaml
