codecov:
  notify:
    after_n_builds: 2

coverage:
  status:
    project:
      default:
        threshold: 0.1%
    patch:
      default:
        target: 80%

ignore:
 - "examples"
 - "fixtures"
 - "testdata"
 - "docs"
 - ".claude"
 - "hack"
 - "**/*.md"
 - "**/*.gotmpl"
